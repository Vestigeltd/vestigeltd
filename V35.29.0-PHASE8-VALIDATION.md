# V35.29.0 Phase 8 validation

## Approved comparison artwork
- Exact user-approved BC10000 artwork installed without edits.
- Exact user-approved ELFA MASTER artwork installed without edits.
- Both assets are 1122 × 1402 pixels.
- SHA-256 hashes are locked in the automated verification suite.
- Comparison-card CSS uses the same 1122:1402 aspect ratio and `object-fit: contain`.
- Runtime transforms, filters, padding, cropping and duplicate status overlays are disabled.
- Card background is explicitly white.

## Regression validation
- `npm run check`: PASS.
- Fulfilment/payment regression suite: PASS.
- Image geometry/background-corner integrity: PASS.
- ZIP extraction/integrity: PASS.

## Cloudflare packaging
Wrangler is not installed in the build container, so `npm run dry-run` must be run from the user's verified local development tree before preview upload.
